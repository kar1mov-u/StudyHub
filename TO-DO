Do not like how admin have access to all the to the academic-term, there should be just 1 button. Start-new Term. Which creates the New-term, makes it active, creates module-run for each modules in the system